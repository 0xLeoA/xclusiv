const ERC20ABI = require("./ERC20ABI.json")
const chainidtodata = require("./chainidtodata.json")
const xclusivabi = require("./xclusivabi.json")
const nftabi = require('./nftabi.json')
const lockabi = require('./lock_abi.json')

module.exports = {
    ERC20ABI,
    chainidtodata,
    xclusivabi,
    nftabi
}